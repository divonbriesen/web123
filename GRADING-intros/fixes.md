# Fixes To Consider

- shared_introductions_instructions.html: The sample section uses `Primary Computer` while the required list uses `Primary Work Computer` and a separate `Operating System & Version` item; make the sample match the stated requirements.
- shared_introductions_instructions.html: The line `If not, remove bullet entirely.` reads awkwardly; change it to `If not, remove the bullet entirely.`
- shared_introductions_instructions.html: The sample course line says `ITIS3135 - Front-End Web Development`, which may be inconsistent with the course naming used elsewhere.
- personal_journal_instructions.html: The page is almost entirely a layout sample; add a few short instruction bullets explaining how to create the root post, where weekly entries go, and how replies should be nested.
- personal_journal_instructions.html: The long `pre` block is doing all the instructional work by itself, which makes the page feel more like raw formatting than clear instructions.
- work_log_instructions.html: Like the journal page, this page would benefit from a few plain-language bullets before the sample so students know the actual rules before reading the tree diagram.
- work_log_instructions.html: The `pre` sample is readable, but it is very dense and may be hard to scan without a short summary of the required heading, time, location, and task pattern.
- firsthtml.html: In `02.html: Header, Main, And Footer`, the sentence `Put place a nav after the h1` has a typo; it should be `Place a nav after the h1`.
- firsthtml.html: In `01.html: Bare Minimum Page`, the title-format instruction is awkwardly phrased; it would be clearer with a concrete example.
- firsthtml.html: The shared-header/shared-footer section repeats `header and footer` several times in a tight span; tighten the wording for readability.
- commandments.html: `The title element shall comprise of the h1 + divider + h2.` uses the wrong phrase; `comprise of` should be revised.
- commandments.html: `Test test test- test readabililty, test on different devices, test with your grandma.` contains a typo in `readabililty`.
- commandments.html: `Test test test- ...` also has awkward punctuation and spacing; rewrite it as a cleaner sentence or list item.
- commandments.html: The long rule about `h1`, `h2`, `main`, and `sections` is correct in intent but overloaded in one sentence; split it for readability.